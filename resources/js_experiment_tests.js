/**
 * test program 1
 */
function isPalindrome() {


}

console.log(isPalindrome("The red fox"));

//sorting algorithm

/**
 *  test 2
    searches a string and pushes results into an array. just reverses?
    --------------
    Output = 'r';
    --------------
 */

var result = [];
var string = 'Lorem ipsum';
//search for all occurences of

function searchString() {
    for(var i = 0; i <= string.length; i++) {
        values.push(i);
    }

    console.log(result[2]);

}

search();
//0, 1, 2, 3, 4
//only push some indexes?

/**
 *  Conditional test. output of variable is dependant on a switch test
    Output = 'Anders'
 */

function name(value) {
    var results;

    switch (value.toLowerCase()) {
        case 'a':
            results = 'Ola';
            break;
        case 'b':
            results = 'Per';
            break;
        case 'c':
            results = 'Anders';
            break;
        default:
    }

    return results;

}

// what is the output of?
console.log(name('C'));
//what is the value of output?
var output = name('C');



// duplicate file = kan ha mal
// ctrl and arrows to move line

// need to realize starts at 1
//

var counter = 0;

for(var i = 1; i <= 2; i++) {
    for(var i = 1; i <= 10; i++) {
        counter++;
    }
}

console.log(counter);
//Output = 20

// starts at 1
// loops twice in total 20

// need to take into account that it is harder to understand others code because of meaning
// want to record the result, because i want to know they understood
//take time with phone in pilot test and if success
//loops quite hard to read
//two loops i and j


// if statements and print

function test(v) {

    var count = 0;
    while((v/3) < count) {
        //10

        v++; // inside or in for
    }

}

test(30);



// do you know what array.push() does?

function printToScreen(a) {
    console.log(a)
}



//yes because "filter out" some of the info

//jumping back and forth with eyes and mind, like the first one. the better control, the better to control for variables.
//follows these principles:
//make explicit. when dont have to write = 0. do so.
// someting slightly off, that makes you need to think
// all in one screen
//high need for rela can make it hard to work with those high need for achievement
// something confusing
// no functions not know because want them to know everything
//when something is off, not hard, it makes you think
// while loop/for loop. increment a var untill the loops end and then display the nr at the end
// write to file
// search file
// check for type
// no functions that require familiarity
// naming of vars/functions also comments, help understanding


//type of tasks
//searching for error tasks, no comprehension, debugging



//use same font for color coding and non color coding


// becomes tired, gives up. dont understand eacher. higher need is the winner. matters more for him.



// dont use shorthand?
// can eyetracker accept values?
// based on what data I want


var result = [];
var string = 'Lorem ipsum';


function search() {
    for(var i = array.length; i < array.length; i--) {
        values.push(result[i]);
    }

    console.log(result[2]);

}

search();



/**
 *  test 3
    performs arithmetic operation
 */





 /**
  *  test 4
     object task. deaclare object. do something with its variables
  */

var student = {
    id: 20,
    firstname: 'Ben',
    lastname: 'Johnson',
    averageGrade: 'B'
}


student.firstname



//sort list
//popluate list
//replace lowest value
//replace values below a certain amount

//sorting
//algorithm

//go through each
//if it is higher then the next, move it

// add two nr toegther
// devide two nr
// subtrasct them

//what is the value of output?
// var output = name('C');


// function select(list[1..n], k)
//     for i from 1 to k
//         minIndex = i
//         minValue = list[i]
//         for j from i+1 to n
//             if list[j] < minValue
//                 minIndex = j
//                 minValue = list[j]
//                 swap list[i] and list[minIndex]
//     return list[k]

// names gives away meaning
// write what each test

//search to find something specific

//searches string, for/while
//prints untill its false, then exists


//replace i with u?
// if understand how it works you understand the reverse
// array search that goes backwards
//one key thing that you need to figure out

//
// output = true or false
//make more to test them, backup. see what is hard. narrow it down.
